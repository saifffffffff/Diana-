<UserControl x:Class="Diana.WPF.View.Controls.CircularPictureBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             MouseUp="UserControl_MouseUp"
             Cursor="Hand">
    <Grid Background="Transparent">
        <Ellipse x:Name="PictureEllipse" Stroke="Gray" StrokeThickness="2">
            <Ellipse.Fill>
                <ImageBrush x:Name="PictureImageBrush" Stretch="UniformToFill"/>
            </Ellipse.Fill>
        </Ellipse>

        <!-- Plus icon overlay when no image is set -->
        <TextBlock x:Name="PlusIcon" Text="+" FontSize="52" Foreground="#A78BFA" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   FontWeight="Bold"
                   Margin="0,2,0,0"/>
    </Grid>
</UserControl>
